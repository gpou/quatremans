        <ul class="nav pull-right">
          <li><%= link_to t('static_pages.quisom.title'),    quisom_path %></li>
          <li><%= link_to t('coleccions.index.title'),   coleccions_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t("meta.language_name") %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <% I18n.available_locales.each do |locale| %>
                <li>
                  <%# link_to t("meta.language_name", :locale => locale.to_sym), :locale=>locale %>
                  <%= link_to t("meta.language_name", :locale => locale.to_sym), eval("root_#{locale}_path") %>
                </li>
              <% end %>
            </ul>
          </li>
        </ul>
