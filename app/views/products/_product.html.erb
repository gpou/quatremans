<li>
  <%= link_to product.name, product %>
  <% if current_user && current_user.admin? %>
    | <%= link_to t('misc.modify'), edit_product_path(product) %>
    | <%= link_to t('misc.delete'), product, method: :delete, data: { confirm: t('misc.sure') } %>
  <% end %>
</li>