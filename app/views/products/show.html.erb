<% provide(:title, "#{t("products.index.title")} | #{@product.name}") %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= t("products.index.title") %>
        |
        <%= @product.name %>
      </h1>
    </section>
  </aside>
  <div class="span8">
      <h3>Aquí les imatges</h3>
      <ol class="imatges">
      </ol>
  </div>
</div>
<%= link_to t("misc.edit"), edit_product_path(@product), class: "btn btn-large btn-primary" %>
<%= link_to t("misc.delete"), @product, method: :delete, data: { confirm: t("misc.sure") }, class: "btn btn-large btn-primary" %>
